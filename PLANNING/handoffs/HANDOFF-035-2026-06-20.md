# Handoff — after Prompt 035 (2026-06-20)

The exterior world container (WEF-04) is complete. Resume STS at **Prompt 036**.

## 1. Last completed prompt

- **Prompt 035 — Exterior topology, chunks, streaming, coordinate frames (WEF-04).**
  Commit **`2c7c8a6`** (pushed to `main`).

## 2. Next prompt to start

- **Prompt 036 — Interior construction kit + authored camera volumes (WEF-05)**
  (MASTER_ROSTER §6.1). A reusable interior kit (farmhouse rooms, shops, homes,
  public halls, cavern-like enclosures): metric modules for wall/floor/ceiling/
  doorway/window/stair/counter/furniture-clearance/interaction/navigation. Camera
  **volumes** must support profile override, target offset, yaw bounds,
  obstruction mode, blend boundary, priority, safe fallback; adjacent volumes
  blend without oscillation; wall fade/cutaway never exposes a void without a
  deliberate backing treatment. Small-room / corridor / stair / crowded-shop /
  large-hall tests keep character + primary interaction readable on Pixel 5;
  exterior/interior handoff preserves destination anchor, facing, camera intent,
  time, NPC state, return path. (Farmhouse reference *implementation* is Prompt
  046, not 036 — 036 is the kit.)
  - **Reuse what exists:** `CameraVolume` type + the rig's volume blending already
    exist (`src/camera/volumes.ts`, `src/camera/rig.ts`); CameraLabScene's
    `demoVolumes()` shows the override pattern. The interior kit is new graybox
    geometry helpers + authored volumes on top of that, likely a new
    `InteriorLabScene` proving ground (mirror the CameraLab/StreamingLab pattern:
    registry + `dev-route.ts` `DEV_SCENES` + Title dev item + `window.sturdyVolley*`
    debug API + `tests/e2e/*.spec.ts`). Art refs: `sv_map_022_town_interiors_grid`,
    `sv_map_023_farmhouse_interior_starter`, `sv_env_059_player_kitchen_evening`,
    `sv_env_060_bathhouse_interior`.

## 3. Where things are (foundation so far)

- **Camera (WEF-01):** `src/camera/` (profiles/orbit/input/volumes/rig); baselines
  locked in `docs/GAMEPLAY_CAMERA_AND_CONTROLS.md`.
- **Motor (WEF-02):** `src/engine/motor.ts` pure core; physics port
  `src/physics/motor-physics.ts` (Havok + ray-pick); `docs/GAMEPLAY_MOTOR.md`.
- **Interaction (WEF-03):** `src/engine/interaction-targeting.ts`;
  `docs/GAMEPLAY_INTERACTION.md`.
- **Topology/streaming (WEF-04, NEW):** `src/world/topology.ts` (coordinate
  frames, chunk grid, stable `${regionId}#cx,cz` ids, floating origin per
  region), `src/world/streaming.ts` (`StreamingController`: active/keep
  hysteresis, horse-speed directional look-ahead, chunk/mesh/body budgets,
  `safeChunkId` + failure retry, `setRegion` drops old records), `src/world/
  variants.ts` (anchor-invariant tide/season/weather/restoration). Doc:
  `docs/WORLD_TOPOLOGY_AND_STREAMING.md`.
- **Proving grounds:** `src/scenes/CameraLabScene.ts` (`?scene=CameraLab`,
  `window.sturdyVolleyLab`) for camera/motor/interaction; **new**
  `src/scenes/StreamingLabScene.ts` (`?scene=StreamingLab`,
  `window.sturdyVolleyStream`) for streaming, with `?debug=streaming` overlay
  (`src/render/streaming-overlay.ts`). Both e2e'd on both projects.

## 4. Locked decisions / gotchas (do not re-trip)

- **STS = push every prompt** (MASTER_ROSTER §0.5). Push after every prompt
  commit, no re-asking.
- **Chunk size 32 m, activeRadius 2, keepRadius 3** — derived from the exterior
  camera + ~60 m fog horizon. Look-ahead is a chunk-`lead` forward keep-radius
  field (a meters-only projection was swamped by the keep band). `setRegion`
  clears records (two regions share local chunk indices). See the Prompt 035
  Decision record in `DEVLOG.md`.
- **Streaming controller is pure** (`{x,z}`, no Babylon); the scene owns layer
  build/dispose + reports cost via `markLoaded`. Keep that boundary.
- **Dev-scene wiring is four sites:** `registry.ts`, `dev-route.ts` `DEV_SCENES`,
  `TitleScene.ts` (menu item + `onSelect` case), and a `?scene=` route. The
  `?scene=` direct-boot works in the production preview build (PreloadScene
  consumes `requestedDevScene()`), which is what the e2e suite runs against.
- **Run the FULL e2e suite** for scene-lifecycle/registry changes (a new scene
  qualifies), never just the new spec.
- **Two-community model:** Willa Crick @ world origin (0,0); Ballast Bay @
  (256,0); community transition on `x=200` along the Klam-ity River. These are
  the proving-ground placements, not final map metrics (those come 037–039).

## 5. Open threads / deviations

- **§0.11 protected file:** `PLANNING/WORLD_EMBODIMENT_FOUNDATION_PSPR.md` carries
  an **unstaged pre-existing user edit** (reverts its banner from "SUPERSEDED"
  back to "DRAFT — NOT APPROVED FOR EXECUTION" + a small STS-wording tweak). It
  was preserved untouched (not staged, not reverted) per §0.11. Note: this is in
  tension with `MASTER_ROSTER.md`, which treats that file as superseded by the
  active master roster — but the active P-SPR is unchanged. If the user means to
  change which plan is authoritative, confirm before acting; otherwise leave it.
- **Art library grew to 96 images** (handoff-034 said 79). The full
  `art-production/current-direction` set (A–F + work-in-progress) was reviewed in
  full this session. North-star unchanged: Town Market Lane (`sv_env_043/044`);
  fidelity target faceted N64 low-poly; the `work-in-progress/*painterly-drift*`
  variants remain the cautionary over-render. The `sv_map_020-draft` still shows
  a volleyball court → purge if ever used (sports remnants are superseded, §1.4).
- No scope deviations in Prompt 035; all five acceptance criteria met.

## 6. Verify-gate state of the last commit (`2c7c8a6`)

All green, no waivers: `tsc -p tsconfig.json` 0 · `tsc -p tsconfig.node.json` 0 ·
`eslint .` 0 · Vitest **433 passed** · Playwright **167 passed + 1 skipped**
(desktop-only aspect sweep) on both `desktop-chromium` + `mobile-chromium` ·
`validate:assets` 0 · `build` 0 · GitDoctor **100/100**.

## 7. Protected-file / worktree status

`PLANNING/` is tracked. Working tree after the commit: only the §5 pre-existing
user edit to `PLANNING/WORLD_EMBODIMENT_FOUNDATION_PSPR.md` remains unstaged and
intact. Paths were staged explicitly throughout (no `git add -A`).
