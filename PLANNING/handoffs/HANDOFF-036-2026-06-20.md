# Handoff — after Prompt 036 (2026-06-20)

The interior construction kit + authored camera volumes (WEF-05) are complete.
Resume STS at **Prompt 037**. Continues the same STS run as HANDOFF-035 (this
session shipped Prompts 035 + 036).

## 1. Last completed prompt

- **Prompt 036 — Interior construction kit + authored camera volumes (WEF-05).**
  Commit **`42468fd`** (pushed to `main`).
- Earlier this session: **Prompt 035 (WEF-04)** commit `2c7c8a6` + governance
  `10b1e9b`.

## 2. Next prompt to start

- **Prompt 037 — Map metric kit + map schemas (WEF-06a)** (MASTER_ROSTER §6.1).
  Author `docs/world/METRIC_KIT.md` — final dimensions, tolerances, rationale,
  and **camera compatibility** for every kit element: paths, desire lines,
  plazas, farm cells, beds, counters, doorways, rooms, buildings, docks, bridges,
  fences, paddocks, trees, crop clearances, cliffs, slopes, stairs, shorelines,
  wading bands, swim boundaries, cave corridors, encounter rooms, transition
  thresholds, camera/navigation clearance, landmark sightlines. Plus
  **machine-readable map schemas** that validate coordinate frames, chunks,
  anchors, volumes, collision/navigation references, variants, and transitions.
  Acceptance: every route supports player capsule + NPC + relevant animal body +
  camera clearance + mobile legibility; reconcile dimensions against
  `sv_style_007_camera_scale_guide.png` (human 1.7–1.8 m, 1 m farm cells,
  cottage/door proportions) + the theme-03 boards.
  - **Reuse / build on:** the project already uses **Zod** (`src/data/schemas.ts`)
    — the "machine-readable map schemas" should be Zod schemas + a validator +
    unit tests, likely **no new Babylon scene** (lighter than 035/036). Pull
    metrics from `src/world/interior-kit.ts` (036), `src/world/topology.ts`
    (035, chunk/coordinate frames + transitions), and the camera baselines in
    `src/camera/profiles.ts` (030, for camera-clearance / sightline reconciliation).
    The metric kit is the authoritative source the Prompt 038 atlas + 039
    blockouts + 046–049 graybox maps read from.

## 3. Where things are (foundation so far)

- **Camera (WEF-01):** `src/camera/` — profiles/orbit/input/volumes/rig. **Rig
  extended in 036:** authored volumes now apply target-offset / per-volume
  obstruction-mode / safe fallback; `pickVolumeSticky` (blend-boundary
  hysteresis); `getState()` reports the **effective active profile** +
  `activeVolumeId`. `docs/GAMEPLAY_CAMERA_AND_CONTROLS.md` (baselines) +
  `docs/INTERIOR_KIT_AND_CAMERA_VOLUMES.md` (volume contract).
- **Motor (WEF-02):** `src/engine/motor.ts` + `src/physics/`; `docs/GAMEPLAY_MOTOR.md`.
- **Interaction (WEF-03):** `src/engine/interaction-targeting.ts`; `docs/GAMEPLAY_INTERACTION.md`.
- **Topology/streaming (WEF-04):** `src/world/topology.ts`, `src/world/streaming.ts`,
  `src/world/variants.ts`; `docs/WORLD_TOPOLOGY_AND_STREAMING.md`.
- **Interior kit (WEF-05, NEW):** `src/world/interior-kit.ts` (`INTERIOR_METRICS`,
  `RoomSpec`, `validateRoomSpec`, `wallSpans`), `src/render/interior-builder.ts`
  (`buildRoom` → closed-shell rooms + volume + anchors);
  `docs/INTERIOR_KIT_AND_CAMERA_VOLUMES.md`.
- **Proving grounds (all dev-only, both projects e2e'd):** `CameraLabScene`
  (`?scene=CameraLab`, `window.sturdyVolleyLab`), `StreamingLabScene`
  (`?scene=StreamingLab`, `window.sturdyVolleyStream`, `?debug=streaming`),
  `InteriorLabScene` (`?scene=InteriorLab`, `window.sturdyVolleyInterior`).

## 4. Locked decisions / gotchas (do not re-trip)

- **STS = push every prompt** (§0.5). The user reaffirmed "STS from here"
  2026-06-20 — push after every prompt commit, no re-asking.
- **Dev-scene wiring is four sites:** `registry.ts`, `dev-route.ts` `DEV_SCENES`,
  `TitleScene.ts` (menu item + `onSelect` case), `?scene=` route (PreloadScene
  consumes `requestedDevScene()`, works in the production preview build the e2e
  runs against).
- **Run the FULL e2e suite** when a change touches shared camera/rig or scene
  lifecycle/registry — Prompt 036 changed the shared rig; the full suite caught a
  `getState` regression (fixed by seeding `activeProfile` in `setProfile`).
- **Metric thresholds are distinct:** doorway pinch = ≥1.0 m (`doorway.width`);
  open corridor = 1.4 m (`navCorridorWidth`); furniture walkable gap = 0.8 m
  (`furnitureClearance`). Don't conflate (see the Prompt 036 decision record).
- **Closed-shell = the backing treatment** (no separate shroud). The void-test
  (`seesBackingThroughNearWall`) proves it; keep rooms floor+ceiling+4-walls.

## 5. Open threads / deviations

- **§0.11 protected file (STILL OPEN):** `PLANNING/WORLD_EMBODIMENT_FOUNDATION_PSPR.md`
  carries an unstaged pre-existing user edit (banner reverted "SUPERSEDED" →
  "DRAFT — NOT APPROVED FOR EXECUTION" + an STS-wording tweak). Preserved
  untouched across Prompts 035 + 036. It conflicts with `MASTER_ROSTER.md`
  treating that file as superseded by the active master roster (the active P-SPR
  is unchanged). If the user means to change which plan is authoritative, confirm
  before acting; otherwise keep leaving it alone.
- No scope deviations in Prompts 035 or 036; all acceptance criteria met.

## 6. Verify-gate state of the last commit (`42468fd`)

All green, no waivers: `tsc -p tsconfig.json` 0 · `tsc -p tsconfig.node.json` 0 ·
`eslint .` 0 · Vitest **452 passed** · Playwright **181 passed + 1 skipped**
(desktop-only aspect sweep) on both `desktop-chromium` + `mobile-chromium` ·
`validate:assets` 0 · `build` 0 · GitDoctor **100/100**.

## 7. Protected-file / worktree status

`PLANNING/` is tracked. Working tree after the commit: only the §5 pre-existing
user edit to `PLANNING/WORLD_EMBODIMENT_FOUNDATION_PSPR.md` remains unstaged and
intact. Paths staged explicitly throughout (no `git add -A`).
