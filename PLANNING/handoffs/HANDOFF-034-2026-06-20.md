# Handoff — after Prompt 034 (2026-06-20)

Continues the same session as [HANDOFF-031](HANDOFF-031-2026-06-20.md). The motor
block (WEF-02) and the interaction model (WEF-03) are complete. Resume STS at
**Prompt 035**.

## 1. Last completed prompt

- **Prompt 034 — Interaction, facing, and tool-targeting model (WEF-03).** Commit
  **`c531180`** (pushed).
- This session also shipped, all pushed: **032** `a9d8398` (motor terrain), **033**
  `343a6f7` (water + traversal + recovery), plus the earlier 028–031 + governance.

## 2. Next prompt to start

- **Prompt 035 — Exterior topology, chunks, streaming, coordinate frames (WEF-04)**
  (MASTER_ROSTER §6.1). The exterior world container *before* laying out regions:
  chunk dimensions from camera visibility + mobile budgets; **separate** render
  chunks / collision proxies / navigation data / interaction anchors / spawn sets
  / camera volumes / audio zones / persistence ids. Writes
  `docs/WORLD_TOPOLOGY_AND_STREAMING.md`. Must account for **horse-speed traversal**
  (preload sizing) and the Willa Crick ↔ Ballast Bay community transition along the
  Klam-ity River. Debug overlay: chunk bounds / states / region origin / budgets.

## 3. Where things are (foundation so far)

- **Camera (WEF-01):** `src/camera/` (profiles/orbit/input/volumes/rig), baselines
  locked in `docs/GAMEPLAY_CAMERA_AND_CONTROLS.md`.
- **Motor (WEF-02):** `src/engine/motor.ts` pure core (`stepMotor` consumes a
  `MotorEnvironment`: ground+wall+step+ceiling+water); grounding/gravity/slope-slide
  /step-up/wall-slide/ceiling/platform/penetration/OOB-recovery + wade/swim +
  traversal links + `groundedPoseAt`. Physics port `src/physics/motor-physics.ts`
  (Havok primary + ray-pick fallback, general `raycast`). Havok bootstrap
  `src/physics/havok.ts`. Tuning in `docs/GAMEPLAY_MOTOR.md`.
- **Interaction (WEF-03):** `src/engine/interaction-targeting.ts` pure resolver
  (`resolveTarget` + facing + action lifecycle). Tuning in
  `docs/GAMEPLAY_INTERACTION.md`. Legacy `src/engine/interaction.ts` still used by
  FarmScene (migrates in 053).
- **Proving ground:** `src/scenes/CameraLabScene.ts` integrates all of the above;
  `?scene=CameraLab`; `window.sturdyVolleyLab` is the debug surface; everything is
  e2e'd in `tests/e2e/camera-lab.spec.ts` (both projects).
- **Art:** the full `art-production/current-direction` library (79 images) was
  reviewed this session. **Town Market Lane (`sv_env_043`/`044`) is the locked
  north-star for town gameplay feel** (memory `project_town_marketlane_northstar`):
  dense lively market + seasonal-reskin model — load-bearing for Prompt 047 +
  festivals + shops. Fidelity target = faceted N64 low-poly (`sv_map_026`/`027`,
  `sv_style_007`, `sv_player_061`); avoid the `work-in-progress/*painterly-drift*`
  over-rendering. The `sv_map_020-draft` still has a volleyball court → purge if
  ever used.

## 4. Locked decisions / gotchas (do not re-trip)

- **STS = push every prompt** (MASTER_ROSTER §0.5 + memory). Push after every
  prompt commit, no re-asking. Don't hand off the session early while context
  headroom remains (memory `feedback_sts_means_dont_pause`).
- **`createMotorState` copies position by accessor, not spread** (Vector3 getter
  NaN trap — see HANDOFF-031).
- **Run the FULL camera-lab spec before the gate**, never just `--grep` (the spec
  has boot/framing tests a grep run skips).
- Motor core stays pure (`{x,y,z}`); physics isolated behind `MotorPhysics`.
- Havok box colliders are added to standable/obstacle kit meshes via
  `isColliderMesh` (decorative/flat/water/ring meshes are skipped — keep that list
  current when adding lab geometry).

## 5. Verify-gate state of the last commit (`c531180`)

All green, no waivers: `tsc -p tsconfig.json` 0 · `tsc -p tsconfig.node.json` 0 ·
`eslint .` 0 · Vitest **401 passed** · Playwright **149 passed + 1 skipped**
(desktop-only aspect sweep) on both projects · `validate:assets` 0 · `build` 0 ·
GitDoctor **100/100**. Havok backend confirmed active in the preview build.

## 6. Protected-file / worktree status

`PLANNING/` is tracked; working tree clean after each prompt. Paths staged
explicitly throughout (no `git add -A`).
