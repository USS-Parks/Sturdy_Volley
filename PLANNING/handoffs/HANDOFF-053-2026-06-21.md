# Handoff — after Prompt 053 (2026-06-21)

**The WEF foundation block (Prompts 028–053) is COMPLETE.** This session shipped
Prompts **044–053** (continues the STS run of HANDOFF-043, which shipped 037–043).
The foundation phase is wrapped; the next phase is the **gameplay continuation**.

## 1. Last completed prompt

- **Prompt 053 — Migrate current scenes + close the foundation phase (WEF-13).**
  Commit **`f1491d3`** (pushed). Docs-only close (README, ARCHITECTURE,
  MASTER_ROSTER, CLAUDE.md, DEVLOG) + the full close gate.
- This session, all pushed: **044** mount system (`e6551fc`), **045** flora
  (`ef74b59`), **046** farm+farmhouse (`31030db`), **047** town (`f87e0d0`),
  **048** river corridor + mounted traversal (`2e17ee6`), **049** cavern
  (`fa529f3`), **050** asset contract+validator (`b78db65`), **051** swap factories
  (`d841087`), **052** perf/accessibility gate (`8d15286`), **053** close
  (`f1491d3`).

## 2. Next prompt to start

- **Prompt 054 — Quest system (legacy 028)** — the start of the **gameplay
  continuation** (054–076), a new phase. MASTER_ROSTER §6.2 carries the legacy
  acceptance; full long-form context in `STURDY_VOLLEY_PSPR.md` §8.2. ≥12 quests
  across farming/fishing/crafting/mining/foraging/exploration/social; touch-friendly
  quest UI; failed timed quests don't break story paths.
- **Note:** this is a phase boundary. The STS the user commenced ("Prompt 43/44")
  drove the **foundation** phase to its wrap (053). Treat starting the gameplay
  continuation as the user's call — confirm before barrelling into 054 unless they
  re-affirm STS for the new phase.

## 3. Where things are (the foundation)

- **Camera** `src/camera/` · **Motor** `src/engine/motor.ts` + `src/physics/` ·
  **Interaction** `src/engine/interaction-targeting.ts` · **Navigation**
  `src/engine/navigation.ts` + `nav-avoidance.ts` + `npc-sim.ts` · **World**
  `src/world/` (topology/streaming/interior-kit/metric-kit/map-schema/atlas/
  blockouts/region-transition) · **Fauna/flora/mount** `src/engine/animal-families.ts`
  + `fauna-behavior.ts` + `flora-motion.ts` + `mount.ts` + `src/render/horse-graybox.ts`.
- **Production maps** (shared stack, real transitions): `BreakpointFarmScene`,
  `FarmhouseInteriorScene`, `BallastBayTownScene`, `KlamityRiverScene`,
  `RainhallCavernScene`. **Proving grounds:** Camera/Streaming/Interior/Nav/Fauna/
  Wild/Mount/Flora/AssetSwap labs.
- **Asset pipeline** `src/render/asset-contract.{json,ts}` + `asset-factory.ts` +
  `asset-fixtures.ts`. **Foundation gate** `src/engine/foundation-budget.ts` +
  `quality-tiers.ts` + `accessibility.ts` + `foundation-coverage.ts`; doc
  `docs/SCALE_AND_PERFORMANCE.md`.

## 4. Locked decisions / gotchas (do not re-trip)

- **Region transitions** use `RegionTransitionData` + `SceneManager.goTo`; clock +
  NPC token cross unchanged. The world graph wired so far: farm↔farmhouse,
  farm↔town, farm↔river, river→town, town↔cavern.
- **Cross-spec global decls:** each e2e spec declares only its own
  `window.sturdyVolley*`; reuse another spec's global (don't redeclare — TS merges
  global interfaces and mismatched shapes break the build, see 047/049).
- **Stale-preview trap:** Playwright `reuseExistingServer:true` reuses a running
  `:4173` preview; kill it before an e2e run after adding a scene, or it serves a
  pre-change `dist/` (cost a debug cycle in 045/047).
- **Mirror OoT/Epona *feel*, never SoH/decomp source** (user reaffirmed 2026-06-21;
  memory `feedback_oot_feel_not_source`). Horse-feel knobs are data — tune to taste.
- **World is far larger than two towns** (memory `project_world_scope_larger`):
  more regions + sailing/glider/train/drift-boat transport + a master narrative the
  user owns. Keep the foundation general; never design narrative.

## 5. Open threads / deviations

- Legacy gameplay scenes (Farm/Town/Interior/Beach/Mine) are **not yet
  content-migrated** onto the foundation maps — that is 054+ work (migrate, not
  discard; retirement criterion in `docs/ARCHITECTURE.md`).
- No production `.glb` yet — contract + swap factory ready; all finished art deferred.
- Real-device FPS is a manual check (CI measures mesh/draw under SwiftShader).
- Willa Crick has no dimensioned layout board; its atlas entry stays provisional.

## 6. Verify-gate state of the last commit (`f1491d3`)

All green, no waivers: `tsc -p tsconfig.json` 0 · `tsc -p tsconfig.node.json` 0 ·
`eslint .` 0 · Vitest **617 passed** · Playwright **293 passed + 1 skipped**
(desktop-only aspect sweep) on both projects (full suite = clean build + preview
smoke) · foundation gate green · `validate:assets` 0 · clean `build` 0 · GitDoctor
**100/100**.

## 7. Protected-file / worktree status

`PLANNING/` tracked. Branch `main`, level with `origin/main`. The §0.11 protected
user file `STURDY_VOLLEY_IMAGE_PROMPT_ROSTER.md` was not touched this session and
has no pending changes. All staging was by explicit path (no `git add -A`).
