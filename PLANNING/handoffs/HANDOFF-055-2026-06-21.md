# Handoff — after Prompt 055 (2026-06-21)

This session shipped the **gameplay-continuation** Prompts **054** (quest system)
and **055** (community restoration projects) on top of the WEF foundation, plus a
four-bug **mobile playability hotfix**. The foundation phase (028–053) was closed
in the prior session (HANDOFF-053). Execution stops here at a clean prompt boundary
by the user's call ("Wrap the session here"); the next prompt is **056**.

## 1. Last completed prompt

- **Prompt 055 — Community restoration projects (legacy 029).** Commit
  **`524d976`** (pushed). Civic project board + phased item/money/relationship
  contributions + opening ceremonies + visible map/schedule changes. See the
  DEVLOG entry for the full surface.
- Also shipped + pushed this session: **Prompt 054** quest system (`63a82b6`),
  the menu-overflow fix (`9c769ab`), and the mobile-play fix (`ba64b8f`).

## 2. Next prompt to start

- **Prompt 056 — Festivals phase one (legacy 030).** Seasonal festival framework +
  **Spring Seed Blessing, Summer Glowtide Night, Fall Harvest Fair** (the first 3
  of the 4 festivals already in `src/data/content/festivals.json`; Frostlight is
  057). Acceptance (MASTER_ROSTER §6.2): festival days alter schedules/shops/map
  setup/music; each festival has ≥1 non-sport minigame + special shop + relationship
  opportunity; multiplayer hooks considered.
- **It is a heaviest-class prompt** (§0.12 flags festival-tier prompts as
  "a whole session each") — budget a full session for it.
- **The festival surface is already mapped** (do not re-explore from scratch):
  - `festivalOn(time, festivals)` (`src/engine/timeSystem.ts:88`) detects today's festival.
  - `byFestival` schedule layer exists (`src/engine/npcSchedule.ts` `pickLayer`,
    2nd precedence after `byEvent`); TownScene `scheduleContext()` currently passes
    `festivalId: null` — wire it to `festivalOn(this.clock.time, content.festivals)?.id`.
  - `restockShop()` + `showShopPanel()` for a festival special stall; `isShopOpen(hours, minutes, festivalActive)` closes regular shops when `festivalActive` (TownScene passes `false` today).
  - Audio is a single one-shot `playReadyChime()` in `src/audio/cues.ts` — add festival music cues there (mirror the WebAudio pattern; no music manager yet).
  - Minigame precedent: `startMinigame`/`stepMinigame` in `src/engine/fishing.ts`
    (pure state→stepped state with caught/lost) — mirror for a deterministic,
    seed-driven, e2e-testable festival minigame.
  - Mirror the 054/055 architecture: rich `festivalSchema` (extend with optional
    minigame/stall/relationship fields so the thin Frostlight entry still parses) +
    pure `festival.ts` engine + defaulted `save.festivals` record + overlay panel +
    TownScene festival-day hosting + unit & e2e.

## 3. Locked decisions / gotchas this session (do not re-trip)

- **Rich-schema-in-content-pipeline pattern** for quests (054) + projects (055):
  extend the Zod schema in `schemas.ts`, JSON content in `content/*.json`, validate
  refs in `content.ts` `checkReferences`, add the collection to `EMPTY_RAW` in
  `tests/unit/content.test.ts`. Festivals should follow the same shape.
- **No `SAVE_VERSION` bump** for new save state — `quests`, `projects` are
  **defaulted** record fields (v3 saves parse to `{}`); real migration is Prompt 067.
  `festivals` should be a defaulted field too.
- **Shared reward granting** lives in `src/engine/rewards.ts` (`grantRewards`);
  quests + projects both delegate. Reuse it for festival rewards.
- **Civic schedule change** is keyed off `civic:<id>` via the existing `byEvent`
  layer (mara-vale `byEvent` in schedules.json). Festivals use the parallel
  `byFestival` layer keyed by the festival **id**.
- **e2e global-collision rule:** specs cast `window.sturdyVolley*` via
  `as unknown as { ... }` with a LOCAL interface (civic.spec) OR extend the single
  canonical declaration (farm.spec owns `sturdyVolleyDebug`). Don't redeclare a
  global with a different shape (TS merge breaks the build).
- **Mobile (post-hotfix):** only **FarmScene** has touch controls (drag-to-move +
  tap-to-interact) + the camera-after-skip fix. Town/Beach/Mine/Interior still have
  **no touch movement/interact** — that's the spawned follow-up + Prompt 068 scope.
  The legacy scene cameras are landscape-tuned; portrait framing relies on the
  `endCutscene` lockedTarget restore (now fixed for Farm).
- **Stale-preview trap** still applies: kill any `:4173` preview before an e2e run
  after changing a scene/build.

## 4. Open threads / deviations

- **Touch parity for non-Farm scenes** — spawned background task
  (`task_737a8b90`, "Extend touch controls to all scenes"); natural Prompt-068 work.
- 055's visible map changes are graybox meshes in TownScene; the
  lighthouse/marsh region scenes those projects "unlock" are future work. The 055
  schedule change is asserted at the flag level in e2e (byEvent precedence is
  unit-tested in `npcSchedule`), not by walking an NPC to its festival waypoint.
- No production `.glb` yet; everything is graybox (art is Prompts 062–063).

## 5. Verify-gate state of the last commit (`524d976`)

All green, no waivers: `tsc -p tsconfig.json` 0 · `tsc -p tsconfig.node.json` 0 ·
`eslint .` 0 · Vitest **656 passed** · Playwright **307 passed + 1 skipped**
(desktop-only aspect sweep) on `desktop-chromium` + `mobile-chromium` ·
`validate:assets` 0 · clean `build` 0 · GitDoctor **100/100**.

## 6. Protected-file / worktree status

Branch `main`, level with `origin/main` after the final session-doc push. The §0.11
protected user file `STURDY_VOLLEY_IMAGE_PROMPT_ROSTER.md` was **not touched** this
session and has no pending changes. All staging was by explicit path (no
`git add -A`). Diagnostic scripts created during the mobile debug
(`scripts/diag-*.mjs`, screenshots) were deleted before committing.
