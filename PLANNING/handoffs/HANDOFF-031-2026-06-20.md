# Handoff — after Prompt 031 (2026-06-20)

Same conversation as [HANDOFF-030](HANDOFF-030-2026-06-19.md); the session
continued past the camera gate at the user's direction (governance fix +
Prompt 031). Resume STS at **Prompt 032**.

## 1. Last completed prompt

- **Prompt 031 — Havok adapter + kinematic capsule motor core (WEF-02a).** Commit
  **`ddc79c3`** (pushed to `main`).
- Also this session: **028** `455ab7a`, **029** `38fbdce`, **030** `39cd4d0`
  (camera gate), pre-step **`9bb1200`** (ORI-001), governance **`752e04e`**
  (STS=push-per-prompt + PLANNING tracking + plan activation). All pushed
  through `ddc79c3`.

## 2. Next prompt to start

- **Prompt 032 — Motor terrain handling and recovery (WEF-02b)**
  (MASTER_ROSTER §6.1).
- Scope: slope limit, sliding, step offset, stairs, low-ceiling handling, stable
  pushing, moving-platform contact contract, penetration recovery, out-of-bounds
  recovery. Document step height / slope limit / recovery thresholds in m/s.
  Deterministic unit coverage per case + proving-ground Playwright on both
  projects.
- This is where **Havok earns its keep**: 031 only grounds on the flat plane via
  a downward ray. 032 adds the **shape-sweep collide-and-slide** (Havok capsule
  cast) for walls/slopes/steps + per-obstacle static colliders on the kit
  stations (slope ramp, 8-step stairs, cliff, walls, cave — already built in the
  proving ground at `src/scenes/CameraLabScene.ts`).

## 3. Where things are (031 architecture)

- **Pure motor core:** `src/engine/motor.ts` (`stepMotor`, `DEFAULT_MOTOR_CONFIG`,
  `GroundHit`). No Babylon import. Extend the core here for slope/step logic; keep
  it pure + unit-tested (`tests/unit/motor.test.ts`).
- **Physics port:** `src/physics/motor-physics.ts` — `MotorPhysics.groundProbe`
  with `HavokMotorPhysics` (primary) + `RaypickMotorPhysics` (fallback). 032 will
  widen this port with a shape-sweep/horizontal-collision method; add it to both
  backends (Havok shape cast; ray-pick can approximate or no-op).
- **Havok bootstrap:** `src/physics/havok.ts` (`initHavok`, `enableScenePhysics`,
  `WORLD_GRAVITY`). `@babylonjs/havok` 1.3.12 is in `optimizeDeps.exclude`
  (vite.config.ts) — keep it there.
- **Integration:** `CameraLabScene.stepMotorFrame()` + `initPhysics()`. The lab
  adds a static ground `PhysicsAggregate` when Havok loads; 032 adds aggregates
  to the standable kit meshes.
- **Docs:** `docs/GAMEPLAY_MOTOR.md` (locked tuning, m/s). Update it with the 032
  thresholds.
- **Debug API:** `window.sturdyVolleyLab` — `motor()`, `controller()`,
  `physicsBackend()`, `dropPlayer(x,y,z)`, `setPlayer(x,z)`, `focus(stationId)`.

## 4. Locked decisions / gotchas (do not reopen / re-trip)

- **Havok inside Babylon.js** is the chosen physics layer (user-confirmed
  2026-06-20). Primary backend; ray-pick is the fallback. Don't drop Havok.
- **`createMotorState` must copy position via accessors, not spread.** A Babylon
  `Vector3` has getter x/y/z; `{ ...v }` copies `_x/_y/_z` and leaves x/y/z
  undefined → NaN that also poisons the camera's smoothed follow position (blank
  canvas). Fixed in 031 — keep it that way; pass plain `{x,y,z}` or rely on the
  accessor copy.
- **Run the FULL camera-lab spec (boot + framing + motor), not just `--grep`.**
  The 031 NaN regression hid because only motor-grep tests were run after the
  integration; boot/framing weren't. Always run the whole spec before the gate.
- Motor tuning (gravity −22, capsule 1.8×0.4, skin 0.08, snap 0.35, turn 12
  rad/s) is locked in `DEFAULT_MOTOR_CONFIG` + `docs/GAMEPLAY_MOTOR.md`.

## 5. Verify-gate state of the last commit (`ddc79c3`)

All green, no waivers: `tsc -p tsconfig.json` 0 · `tsc -p tsconfig.node.json` 0 ·
`eslint .` 0 · Vitest **373 passed** · Playwright **131 passed + 1 skipped**
(desktop-only aspect sweep) on both projects · `validate:assets` 0 · `build` 0 ·
GitDoctor **100/100**. Havok backend confirmed active in the preview build.

## 6. Protected-file guard status

`PLANNING/` is now **tracked** (committed in `752e04e` at the user's explicit
instruction). The earlier §0.11 "protected user changes" (CLAUDE.md / PSPR /
image-prompt roster edits) were the plan-activation edits and were committed in
`752e04e`. Working tree is clean aside from this handoff + the session-log update
being committed now. No `git add -A` was used; paths staged explicitly throughout.

## 7. Push policy (now locked)

**STS = push every prompt** (MASTER_ROSTER §0.5 + `feedback_sts_push_every_prompt`
memory). After each prompt's gate + DEVLOG + commit, run `git push origin main`.
No re-asking between prompts. No `--force` without explicit instruction.
