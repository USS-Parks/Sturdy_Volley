# Handoff — after Prompt 030 (2026-06-19)

First STS session of the [MASTER_ROSTER.md](../MASTER_ROSTER.md). The camera gate
(WEF-01, Prompts 028–030) is complete. Handing off before **Prompt 031 (Havok
adapter + kinematic capsule motor core)**, which §0.12 explicitly names as a
session-heavy prompt that starts fresh.

## 1. Last completed prompt

- **Prompt 030 — Lock camera baselines, reduced motion, telemetry, decision
  record (WEF-01c).** Commit **`39cd4d0`**.
- Also landed this session: **028** (`455ab7a`, TS config split + camera
  proving-ground shell), **029** (`38fbdce`, camera profiles/rig/obstruction/
  input), and a pre-step **`9bb1200`** (ORI-001 originality fix — see §3).

## 2. Next prompt to start

- **Prompt 031 — Havok adapter + kinematic capsule motor core (WEF-02a)**
  (MASTER_ROSTER §6.1).
- Setup it expects:
  - Verify the current official Babylon/Havok APIs **against the pinned package**
    before integrating (the prompt says so explicitly). `@babylonjs/core` is
    `^7.0.0`; `@babylonjs/havok` is **not yet a dependency** — adding it is part
    of 031. Check the installed core version (`npm ls @babylonjs/core`) and match
    the Havok plugin version to it.
  - The motor is built + proven in the **CameraLabScene** proving ground. It
    currently has a **proxy planar driver** (`drivePlayer()` in
    `src/scenes/CameraLabScene.ts`) that moves `playerMesh` at a fixed speed and
    feeds `playerVel` to the camera. **Prompt 031/033 replaces this proxy with
    the real kinematic-capsule motor** — keep `playerVel`/`FollowTarget` wired so
    the camera look-ahead keeps working.
  - Existing stamina/gait behaviour lives in `src/engine/controller.ts`
    (`stepController`, pure). 031 must keep it functional through the adapter.

## 3. Locked decisions made this session (do not reopen)

- **TS config split:** `tsconfig.base.json` (all strict flags, single source of
  truth) + `tsconfig.json` (game, `src`, DOM + vite/client) + `tsconfig.node.json`
  (tooling + `tests`, node + vite/client). `npm run typecheck` runs both; `build`
  runs typecheck then vite. Don't move strict flags out of the base.
- **GitDoctor PKG-002** now follows one level of `extends` so strictness in
  `tsconfig.base.json` is recognised (`tools/local_gitdoctor_scan.py`).
- **Dev scene routing:** `?scene=CameraLab` (allow-listed in
  `src/scenes/dev-route.ts`) is the production-preview-safe boot route; Title has
  a dev-only "Dev · Camera Lab" item. Future dev scenes join the `DEV_SCENES`
  allow-list.
- **Camera (the gate):** all baselines + the obstruction/reduced-motion rules are
  LOCKED in `docs/GAMEPLAY_CAMERA_AND_CONTROLS.md`. Baseline = the `standard`
  variant per context. `fade` is the chosen occluder rule, `cutaway` the
  recorded fallback. The camera rig holds no tuning — retune via
  `src/camera/profiles.ts` data only. **No production map metric is finalised
  before this gate (now done).**
- **ORI-001 pre-step:** the rewritten GitDoctor scanner (`caf8bd1`) introduced a
  pre-existing HIGH finding (two `tests/unit` descriptions naming a franchise),
  which blocked *every* commit's verify gate. Cleared in `9bb1200` by renaming
  the two descriptions to neutral phrasing. The DEVLOG had already flagged this
  as needing a cleanup commit.

## 4. Open threads / risks / deviations

- **Push policy conflict (needs a user word):** the recalled memory
  `feedback_sts_push_every_prompt` says STS pushes every prompt, but
  MASTER_ROSTER **§0.5** (the newest approved doc, which wins over conflicting
  docs) says *push only when explicitly asked* and *never volunteer an
  unrequested push*. I followed §0.5 — **nothing has been pushed this session.**
  Commits `9bb1200`, `455ab7a`, `38fbdce`, `39cd4d0` are local on `main`. If the
  user wants push-per-prompt, they should say so (and the stale memory should be
  updated). Until then, hold pushes.
- No scope deviations. The camera proving ground's proxy player driver is an
  intentional stand-in for the 031 motor (documented above).
- `npm run build` prints a chunk-size > 4000 kB warning (Babylon). Pre-existing,
  not a gate failure; relevant to the eventual mobile/PWA prompt (068).

## 5. Verify-gate state of the last commit (`39cd4d0`)

All green, no waivers: `tsc -p tsconfig.json` 0 · `tsc -p tsconfig.node.json` 0 ·
`eslint .` 0 · Vitest **364 passed** · Playwright **127 passed + 1 skipped**
(desktop-only aspect sweep) on desktop-chromium + mobile-chromium ·
`validate:assets` 0 · `build` 0 · GitDoctor **100/100**, `--fail-on high` exit 0.

## 6. Protected-file guard status

The §0.11 pre-existing user changes are intact and **unstaged**: `CLAUDE.md`,
`STURDY_VOLLEY_IMAGE_PROMPT_ROSTER.md`, `STURDY_VOLLEY_PSPR.md` (all ` M`), and
the untracked `PLANNING/` tree (which holds `MASTER_ROSTER.md` + this handoff).
Every commit this session staged explicit paths only — no `git add -A`. Confirm
again at next-session bootstrap.
