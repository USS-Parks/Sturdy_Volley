# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
.vite/

# Test artifacts
coverage/
playwright-report/
test-results/
blob-report/
.playwright/

# Local GitDoctor scan scratch dir (see tools/local_gitdoctor_scan.py + PSPR §0.2 step 7)
.tmp/

# Editor / OS
.DS_Store
*.local
*.log
Thumbs.db

# Env
.env
.env.*
!.env.example

# Claude Code worktrees + agent scratch nested under this folder (unrelated repo)
.claude/

# Working art-production folder (source art / style boards), generated separately.
# Excluded until the art is ready to apply; runtime-ready assets live in public/assets/.
art-production/
